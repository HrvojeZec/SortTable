// (GET) api/clubs


//const databaseData = // database.js -> poziv na Firebase

//const search(term) => {
// Machester City -> term: it
// SELECT * FROM klubovi WHERE name LIKE '%it%' // MySQL
// clubs.find(club => club.name.includes(term));
//}

